$kind: http-request
url: "{{baseUrl}}/api/v1/orders/{{orderId}}"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
      pm.test("Order ID matches", () => {
          const json = pm.response.json();
          pm.expect(json.data.order_id).to.equal(pm.collectionVariables.get("orderId"));
      });
    language: text/javascript
order: 3000
